{
  "type": "object",
  "properties": {
    "job_related": {
      "type": "boolean",
      "description": "True if the email content is related to a job application process."
    },
    "empresa": {
      "type": "string",
      "description": "The name of the company."
    },
    "cargo": {
      "type": "string",
      "description": "The name of the job role."
    },
    "resultado": {
      "type": "string",
      "description": "The outcome of the application status.",
      "enum": [
        "rejeitado",
        "avancou",
        "indefinido"
      ]
    },
    "proxima_etapa": {
      "type": "string",
      "description": "Short description of the explicit next step when resultado is avancou, or an empty string when not stated."
    }
  },
  "required": [
    "job_related",
    "empresa",
    "cargo",
    "resultado",
    "proxima_etapa"
  ]
}
